import React, { FC } from "react";
import Head from "next/head";

const Home: FC = () => (
  <div>
    <Head>
      <title>Mimo mocks</title>
    </Head>
    <h1>Im the home</h1>
  </div>
);

export default Home;
